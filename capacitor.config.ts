import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.softour.maasfrontend',
    appName: 'maas-fe-mobile-template-public',
    webDir: 'www',
    android: {
        adjustMarginsForEdgeToEdge: 'auto',
    },
};

export default config;
